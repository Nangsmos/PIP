#include<iostream>
using namespace std;
int main(){
	bool found = false;
	while(!found){
		found = false;
		cout<<" found = "<<found;
		break;
		if(found){
			found = true;
			cout<<" found = "<<found;
			break;
		}
	}
}
